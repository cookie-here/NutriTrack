# NutriTrack System Architecture Map
**Complete Component Connection Mapping**

---

## 🏗️ SYSTEM OVERVIEW

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                 │
│                    (React - Port 5173)                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Pages      │→ │   API Layer  │→ │  HTTP Request│          │
│  │  (UI/Forms)  │  │   (api.js)   │  │  (Fetch API) │          │
│  └──────────────┘  └──────────────┘  └──────┬───────┘          │
└─────────────────────────────────────────────┼───────────────────┘
                                              │
                                              │ JSON over HTTP
                                              │
┌─────────────────────────────────────────────▼───────────────────┐
│                         BACKEND                                  │
│                   (Express.js - Port 8000)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Routes     │→ │ Controllers  │→ │   Models     │          │
│  │ (endpoints)  │  │  (business)  │  │ (Sequelize)  │          │
│  └──────────────┘  └──────────────┘  └──────┬───────┘          │
└─────────────────────────────────────────────┼───────────────────┘
                                              │
                                              │ SQL Queries
                                              │
┌─────────────────────────────────────────────▼───────────────────┐
│                        DATABASE                                  │
│                    (SQLite - db.sqlite)                          │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Tables: users, growth_records, reminders,               │   │
│  │          emergency_contacts, partners                     │   │
│  └──────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📱 FEATURE 1: AUTHENTICATION

### Frontend → Backend → Database

```
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND                                                         │
├─────────────────────────────────────────────────────────────────┤
│ Pages:                                                           │
│   • Login.jsx        - Login form                                │
│   • Signup.jsx       - Registration form                         │
│   • StageSelect.jsx  - User type selection                       │
│                                                                  │
│ API Functions (api.js):                                          │
│   • login({ email, password })                                   │
│   • register({ fullName, email, dueDate, userType, ... })       │
│   • getCurrentUser()                                             │
│   • setAuthToken(token) / getAuthToken() / clearAuthToken()      │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ POST /api/auth/login
                       │ POST /api/auth/register
                       │ GET  /api/auth/me
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ BACKEND                                                           │
├───────────────────────────────────────────────────────────────────┤
│ Routes (authRoutes.js):                                          │
│   • POST   /api/auth/login       → login                         │
│   • POST   /api/auth/register    → register                      │
│   • GET    /api/auth/me          → getCurrentUser                │
│                                                                  │
│ Controller (authController.js):                                  │
│   • login()         - Verify credentials, create JWT token       │
│   • register()      - Validate, hash password, create user       │
│   • getCurrentUser()- Get user profile from token                │
│                                                                  │
│ Middleware (auth.js):                                            │
│   • authenticateToken() - Verify JWT, attach user to request     │
│   • hashPassword()      - Bcrypt password hashing                │
│   • verifyPassword()    - Compare hashed passwords               │
│   • createAccessToken() - Generate JWT tokens                    │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ Sequelize ORM
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ DATABASE                                                          │
├───────────────────────────────────────────────────────────────────┤
│ Model: User.js                                                    │
│                                                                  │
│ Table: users                                                     │
│ ┌────┬────────────────┬─────────────────┬──────────┬──────────┐ │
│ │ id │ email          │ hashed_password │ full_name│ user_type│ │
│ │    │ due_date       │ baby_date_of_birth │ ...    │          │ │
│ └────┴────────────────┴─────────────────┴──────────┴──────────┘ │
└───────────────────────────────────────────────────────────────────┘
```

---

## 📊 FEATURE 2: GROWTH TRACKING

```
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND                                                         │
├─────────────────────────────────────────────────────────────────┤
│ Pages:                                                           │
│   • Growth.jsx - Growth tracking dashboard                       │
│                                                                  │
│ Components:                                                      │
│   • GrowthHeader.jsx   - Header with baby info                   │
│   • MilestoneCard.jsx  - Individual milestone display            │
│                                                                  │
│ API Functions (api.js):                                          │
│   • getGrowthRecords()                                           │
│   • createGrowthRecord(recordData)                               │
│   • deleteGrowthRecord(recordId)                                 │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ GET    /api/growth/records
                       │ POST   /api/growth/records
                       │ DELETE /api/growth/records/:recordId
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ BACKEND                                                           │
├───────────────────────────────────────────────────────────────────┤
│ Routes (growthRoutes.js):                                        │
│   • GET    /growth/records           → getGrowthRecords          │
│   • POST   /growth/records           → createGrowthRecord        │
│   • GET    /growth/records/:id       → getGrowthRecord           │
│   • PUT    /growth/records/:id       → updateGrowthRecord        │
│   • DELETE /growth/records/:id       → deleteGrowthRecord        │
│   (All protected with authenticateToken middleware)              │
│                                                                  │
│ Controller (growthController.js):                                │
│   • getGrowthRecords()   - Fetch all records for user            │
│   • createGrowthRecord() - Add new measurement                   │
│   • updateGrowthRecord() - Update existing record                │
│   • deleteGrowthRecord() - Remove record                         │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ Sequelize ORM
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ DATABASE                                                          │
├───────────────────────────────────────────────────────────────────┤
│ Model: GrowthRecord.js                                           │
│                                                                  │
│ Table: growth_records                                            │
│ ┌────┬─────────┬──────┬───────────┬──────────┬──────────────────┐ │
│ │ id │ user_id │ date │ age_months│ weight_kg│ height_cm        │ │
│ │    │         │      │           │          │ head_circumf_cm  │ │
│ └────┴─────────┴──────┴───────────┴──────────┴──────────────────┘ │
│                                                                  │
│ Relationships:                                                   │
│   • belongsTo User (foreign key: user_id)                        │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🔔 FEATURE 3: REMINDERS & NOTIFICATIONS

```
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND                                                         │
├─────────────────────────────────────────────────────────────────┤
│ Pages:                                                           │
│   • Home.jsx - Display reminders on dashboard                    │
│                                                                  │
│ Components:                                                      │
│   • ReminderCard.jsx       - Individual reminder display         │
│   • NotificationCard.jsx   - Notification UI                     │
│   • NotificationBanner.jsx - Permission banner                   │
│                                                                  │
│ Services:                                                        │
│   • NotificationService.js - Browser notification API            │
│                                                                  │
│ API Functions (api.js):                                          │
│   • getReminders()                                               │
│   • createReminder(reminderData)                                 │
│   • completeReminder(reminderId)                                 │
│   • deleteReminder(reminderId)                                   │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ GET   /api/reminders/
                       │ POST  /api/reminders/
                       │ PATCH /api/reminders/:id/complete
                       │ DELETE /api/reminders/:id
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ BACKEND                                                           │
├───────────────────────────────────────────────────────────────────┤
│ Routes (reminderRoutes.js):                                      │
│   • GET    /reminders/              → getReminders               │
│   • POST   /reminders/              → createReminder             │
│   • PATCH  /reminders/:id/complete  → completeReminder           │
│   • DELETE /reminders/:id           → deleteReminder             │
│   (All protected with authenticateToken middleware)              │
│                                                                  │
│ Controller (reminderController.js):                              │
│   • getReminders()     - Fetch all reminders for user            │
│   • createReminder()   - Create new reminder                     │
│   • completeReminder() - Mark reminder as completed              │
│   • deleteReminder()   - Delete reminder                         │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ Sequelize ORM
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ DATABASE                                                          │
├───────────────────────────────────────────────────────────────────┤
│ Model: Reminder.js                                               │
│                                                                  │
│ Table: reminders                                                 │
│ ┌────┬─────────┬───────┬──────────────┬──────┬───────────────┐  │
│ │ id │ user_id │ title │ reminder_date│ type │ completed     │  │
│ │    │         │       │ vaccine_name │ ...  │ status        │  │
│ └────┴─────────┴───────┴──────────────┴──────┴───────────────┘  │
│                                                                  │
│ Relationships:                                                   │
│   • belongsTo User (foreign key: user_id)                        │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🥗 FEATURE 4: NUTRITION & FEEDING

```
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND                                                         │
├─────────────────────────────────────────────────────────────────┤
│ Pages:                                                           │
│   • Nutrition.jsx - Nutrition tips and safe foods                │
│   • Feeding.jsx   - Age-appropriate feeding guide                │
│                                                                  │
│ Components:                                                      │
│   • NutritionHeader.jsx - Header for nutrition page              │
│   • NutritionCard.jsx   - Food/tip card display                  │
│   • FeedingHeader.jsx   - Header for feeding page                │
│                                                                  │
│ API Functions (api.js):                                          │
│   • getNutritionTips()                                           │
│   • getSafeFoods()                                               │
│   • getFeedingGuide()                                            │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ GET /api/static/nutrition-tips
                       │ GET /api/static/safe-foods
                       │ GET /api/static/feeding-guide
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ BACKEND                                                           │
├───────────────────────────────────────────────────────────────────┤
│ Routes (staticRoutes.js):                                        │
│   • GET /static/nutrition-tips  → getNutritionTips               │
│   • GET /static/safe-foods      → getSafeUnsafeFoods             │
│   • GET /static/feeding-guide   → getFeedingGuide                │
│   • GET /static/vaccine-schedule→ getVaccineSchedule             │
│   • GET /static/daily-tip       → getDailyTip                    │
│   (No authentication required - public data)                     │
│                                                                  │
│ Controller (staticController.js):                                │
│   • getNutritionTips()    - Return nutrition advice              │
│   • getSafeUnsafeFoods()  - Return safe/unsafe food lists        │
│   • getFeedingGuide()     - Return age-based feeding info        │
│   • getVaccineSchedule()  - Return vaccine schedule              │
│   • getDailyTip()         - Return random daily tip              │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ Static Data (No Database)
                       │ Returns hardcoded JSON responses
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ NO DATABASE TABLE                                                 │
│ (Static content stored in controller code)                       │
└───────────────────────────────────────────────────────────────────┘
```

---

## 💉 FEATURE 5: VACCINE TRACKING

```
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND                                                         │
├─────────────────────────────────────────────────────────────────┤
│ Pages:                                                           │
│   • Vaccines.jsx - Vaccine tracking dashboard                    │
│   • Home.jsx     - Shows upcoming vaccines                       │
│                                                                  │
│ Components:                                                      │
│   • VaccinesHeader.jsx - Header with stats                       │
│   • VaccineCard.jsx    - Individual vaccine display              │
│                                                                  │
│ API Functions (api.js):                                          │
│   • getVaccineSchedule()                                         │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ GET /api/static/vaccine-schedule
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ BACKEND                                                           │
├───────────────────────────────────────────────────────────────────┤
│ Routes (staticRoutes.js):                                        │
│   • GET /static/vaccine-schedule → getVaccineSchedule            │
│                                                                  │
│ Controller (staticController.js):                                │
│   • getVaccineSchedule() - Return vaccine schedule with details  │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ Static Data (No Database)
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ NO DATABASE TABLE                                                 │
│ (Vaccine schedule stored in controller code)                     │
└───────────────────────────────────────────────────────────────────┘
```

---

## 👤 FEATURE 6: USER PROFILE & SETTINGS

```
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND                                                         │
├─────────────────────────────────────────────────────────────────┤
│ Pages:                                                           │
│   • Profile.jsx - User profile and settings                      │
│                                                                  │
│ API Functions (api.js):                                          │
│   • getUserProfile()                                             │
│   • updateUserProfile(profileData)                               │
│   • saveEmergencyContact(contactData)                            │
│   • getEmergencyContact()                                        │
│   • deleteEmergencyContact()                                     │
│   • sendPartnerInvite(partnerEmail)                              │
│   • getPartnerInvitations()                                      │
│   • acceptPartnerInvitation(invitationId)                        │
│   • declinePartnerInvitation(invitationId)                       │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ GET    /api/profile
                       │ PUT    /api/profile
                       │ POST   /api/profile/emergency-contact
                       │ GET    /api/profile/emergency-contact
                       │ DELETE /api/profile/emergency-contact
                       │ POST   /api/profile/partner-invite
                       │ GET    /api/profile/partner-invitations
                       │ PATCH  /api/profile/partner-invitations/:id/accept
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ BACKEND                                                           │
├───────────────────────────────────────────────────────────────────┤
│ Routes (profileRoutes.js):                                       │
│   • GET    /profile                    → getUserProfile          │
│   • PUT    /profile                    → updateUserProfile       │
│   • POST   /profile/emergency-contact  → saveEmergencyContact    │
│   • GET    /profile/emergency-contact  → getEmergencyContact     │
│   • DELETE /profile/emergency-contact  → deleteEmergencyContact  │
│   • POST   /profile/partner-invite     → sendPartnerInvite       │
│   • GET    /profile/partner-invitations→ getPartnerInvitations   │
│   • PATCH  /partner-invitations/:id/accept → acceptPartnerInvite │
│   (All protected with authenticateToken middleware)              │
│                                                                  │
│ Controller (profileController.js):                               │
│   • getUserProfile()           - Get full user profile + relations│
│   • updateUserProfile()        - Update user info                │
│   • saveEmergencyContact()     - Create/update emergency contact │
│   • getEmergencyContact()      - Fetch emergency contact         │
│   • deleteEmergencyContact()   - Remove emergency contact        │
│   • sendPartnerInvite()        - Send partner sync invite        │
│   • getPartnerInvitations()    - Get partner invitations         │
│   • acceptPartnerInvitation()  - Accept partner request          │
│   • declinePartnerInvitation() - Decline partner request         │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ Sequelize ORM
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│ DATABASE                                                          │
├───────────────────────────────────────────────────────────────────┤
│ Model: EmergencyContact.js                                       │
│ Table: emergency_contacts                                        │
│ ┌────┬─────────┬──────────┬─────────────┬──────────────┐         │
│ │ id │ user_id │ name     │ phone       │ relationship │         │
│ └────┴─────────┴──────────┴─────────────┴──────────────┘         │
│   • belongsTo User (foreign key: user_id)                        │
│   • User.hasOne EmergencyContact                                 │
│                                                                  │
│ Model: Partner.js                                                │
│ Table: partners                                                  │
│ ┌────┬─────────┬───────────────┬────────────┬────────┐           │
│ │ id │ user_id │ partner_email │ partner_id │ status │           │
│ └────┴─────────┴───────────────┴────────────┴────────┘           │
│   • belongsTo User (foreign key: user_id)                        │
│   • belongsTo User (foreign key: partner_id)                     │
│   • User.hasOne Partner                                          │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🏠 FEATURE 7: HOME DASHBOARD

```
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND                                                         │
├─────────────────────────────────────────────────────────────────┤
│ Pages:                                                           │
│   • Home.jsx - Main dashboard                                    │
│                                                                  │
│ Components:                                                      │
│   • GreetingCard.jsx       - Welcome message with trimester info │
│   • ReminderCard.jsx       - Upcoming reminders                  │
│   • TipCard.jsx            - Daily tips                          │
│   • QuickAccessGrid.jsx    - Navigation shortcuts                │
│   • BottomNavigation.jsx   - App navigation bar                  │
│                                                                  │
│ API Functions Used:                                              │
│   • getCurrentUser()    - Get user info for greeting             │
│   • getReminders()      - Show upcoming reminders                │
│   • getDailyTip()       - Display random tip                     │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       │ Multiple API calls from other features
                       │
                       │ GET /api/auth/me
                       │ GET /api/reminders/
                       │ GET /api/static/daily-tip
                       │
                       └→ Uses endpoints from Auth, Reminders, Static
```

---

## 🔐 SECURITY & MIDDLEWARE FLOW

```
┌─────────────────────────────────────────────────────────────────┐
│ REQUEST FLOW WITH AUTHENTICATION                                 │
└─────────────────────────────────────────────────────────────────┘

1. User logs in → Backend creates JWT token → Stored in localStorage
2. Frontend includes token in all protected requests:

   fetch(url, {
     headers: {
       'Authorization': 'Bearer eyJhbGciOiJIUzI1NiIsInR5...'
     }
   })

3. Backend middleware (authenticateToken) runs BEFORE controller:
   
   Request → authenticateToken() → Verify JWT → Attach user to req
                    ↓
              If valid: next() → Controller executes
              If invalid: 401 Unauthorized

4. Controller has access to req.user with authenticated user data

PROTECTED ROUTES (Require Authentication):
  • All /api/growth/* endpoints
  • All /api/reminders/* endpoints
  • All /api/profile/* endpoints
  • GET /api/auth/me

PUBLIC ROUTES (No Authentication):
  • POST /api/auth/login
  • POST /api/auth/register
  • All /api/static/* endpoints
```

---

## 📦 DATABASE SCHEMA & RELATIONSHIPS

```
┌─────────────────────────────────────────────────────────────────┐
│ DATABASE TABLES & RELATIONSHIPS                                  │
└─────────────────────────────────────────────────────────────────┘

users (User.js)
├── id (PK)
├── email (unique)
├── hashed_password
├── full_name
├── due_date
├── user_type (enum: 'pregnant', 'newParent', 'trying')
├── baby_date_of_birth
├── created_at
└── updated_at

growth_records (GrowthRecord.js)
├── id (PK)
├── user_id (FK → users.id)
├── date
├── age_months
├── weight_kg
├── height_cm
└── head_circumference_cm

reminders (Reminder.js)
├── id (PK)
├── user_id (FK → users.id)
├── title
├── reminder_date
├── type
├── completed (boolean)
├── description
├── vaccine_name
├── dose_number
├── recipient (enum: 'baby', 'mother')
├── age_due_months
└── status (enum: 'pending', 'completed', 'overdue', 'skipped')

emergency_contacts (EmergencyContact.js)
├── id (PK)
├── user_id (FK → users.id)
├── name
├── phone
├── relationship (enum: 'spouse', 'parent', 'sibling', 'friend', 'doctor', 'other')
├── created_at
└── updated_at

partners (Partner.js)
├── id (PK)
├── user_id (FK → users.id)
├── partner_email
├── partner_id (FK → users.id, nullable)
├── status (enum: 'pending', 'accepted', 'declined')
├── created_at
└── updated_at

RELATIONSHIPS:
  User ─┬─ hasMany    → GrowthRecords
        ├─ hasMany    → Reminders
        ├─ hasOne     → EmergencyContact
        └─ hasOne     → Partner
```

---

## 📂 FILE STRUCTURE SUMMARY

```
nutritrack/
├── FrontEnd/
│   └── src/
│       ├── api.js                    # Central API layer (all HTTP calls)
│       ├── main.jsx                  # App entry point
│       ├── App.jsx                   # Route definitions
│       │
│       ├── pages/                    # Page components
│       │   ├── Login.jsx            # → /api/auth/login
│       │   ├── Signup.jsx           # → /api/auth/register
│       │   ├── StageSelect.jsx      # User type selection
│       │   ├── Home.jsx             # → /api/auth/me, /api/reminders, /api/static/daily-tip
│       │   ├── Growth.jsx           # → /api/growth/records
│       │   ├── Nutrition.jsx        # → /api/static/nutrition-tips, /api/static/safe-foods
│       │   ├── Feeding.jsx          # → /api/static/feeding-guide
│       │   ├── Vaccines.jsx         # → /api/static/vaccine-schedule
│       │   └── Profile.jsx          # → /api/profile/*
│       │
│       ├── components/              # Reusable UI components
│       │   ├── BottomNavigation.jsx
│       │   ├── ReminderCard.jsx
│       │   ├── GrowthHeader.jsx
│       │   ├── MilestoneCard.jsx
│       │   └── ... (20+ components)
│       │
│       └── services/
│           └── NotificationService.js  # Browser notifications
│
└── backend/
    └── src/
        ├── server.js                 # Express app setup
        │
        ├── config/
        │   └── index.js             # Configuration (DB, JWT, CORS)
        │
        ├── db/
        │   └── sequelize.js         # Database connection setup
        │
        ├── models/                  # Database models (Sequelize)
        │   ├── index.js
        │   ├── User.js              # users table
        │   ├── GrowthRecord.js      # growth_records table
        │   ├── Reminder.js          # reminders table
        │   ├── EmergencyContact.js  # emergency_contacts table
        │   └── Partner.js           # partners table
        │
        ├── routes/                  # API endpoints
        │   ├── authRoutes.js        # /api/auth/*
        │   ├── growthRoutes.js      # /api/growth/*
        │   ├── reminderRoutes.js    # /api/reminders/*
        │   ├── profileRoutes.js     # /api/profile/*
        │   └── staticRoutes.js      # /api/static/*
        │
        ├── controllers/             # Business logic
        │   ├── authController.js
        │   ├── growthController.js
        │   ├── reminderController.js
        │   ├── profileController.js
        │   └── staticController.js
        │
        ├── middleware/
        │   └── auth.js              # JWT authentication & password utilities
        │
        └── utils/
            └── auth.js              # Auth utility functions
```

---

## 🔄 COMPLETE DATA FLOW EXAMPLE

**Example: Creating a Growth Record**

```
1. USER ACTION (Frontend)
   └─ User fills form in Growth.jsx:
      • Age: 6 months
      • Weight: 7.5 kg
      • Height: 67 cm

2. COMPONENT HANDLER
   └─ handleAddRecord() called
      └─ Calls: createGrowthRecord({ age_months: 6, weight_kg: 7.5, height_cm: 67 })

3. API LAYER (api.js)
   └─ Builds HTTP request:
      POST http://localhost:8000/api/growth/records
      Headers: { 'Authorization': 'Bearer token123...', 'Content-Type': 'application/json' }
      Body: { "age_months": 6, "weight_kg": 7.5, "height_cm": 67 }

4. BACKEND ROUTING (server.js → growthRoutes.js)
   └─ Express matches route: POST /api/growth/records
      └─ Runs middleware: authenticateToken
         └─ Verifies JWT, extracts user_id = 5
            └─ Calls controller: createGrowthRecord

5. CONTROLLER LOGIC (growthController.js)
   └─ Receives: req.user.id = 5, req.body = { age_months: 6, ... }
      └─ Calls: GrowthRecord.create({ user_id: 5, age_months: 6, ... })

6. MODEL/ORM (GrowthRecord.js → Sequelize)
   └─ Sequelize generates SQL:
      INSERT INTO growth_records (user_id, age_months, weight_kg, height_cm, date)
      VALUES (5, 6, 7.5, 67, '2026-01-04 13:00:00');

7. DATABASE (SQLite - db.sqlite)
   └─ Executes SQL, returns new record with id = 42

8. RESPONSE CHAIN
   └─ Database → Sequelize → Controller → Express → Frontend
      Response: { "id": 42, "user_id": 5, "age_months": 6, "weight_kg": 7.5, ... }

9. FRONTEND UPDATE
   └─ Growth.jsx receives response
      └─ Updates state: setGrowthRecords([newRecord, ...growthRecords])
         └─ React re-renders UI with new record displayed
```

---

## 🎯 API ENDPOINT SUMMARY

| Method | Endpoint | Controller | Auth | Description |
|--------|----------|------------|------|-------------|
| **AUTHENTICATION** |
| POST | /api/auth/register | authController.register | ❌ | Create new user |
| POST | /api/auth/login | authController.login | ❌ | Login & get JWT |
| GET | /api/auth/me | authController.getCurrentUser | ✅ | Get current user |
| **GROWTH TRACKING** |
| GET | /api/growth/records | growthController.getGrowthRecords | ✅ | List all records |
| POST | /api/growth/records | growthController.createGrowthRecord | ✅ | Add new record |
| DELETE | /api/growth/records/:id | growthController.deleteGrowthRecord | ✅ | Delete record |
| **REMINDERS** |
| GET | /api/reminders/ | reminderController.getReminders | ✅ | List reminders |
| POST | /api/reminders/ | reminderController.createReminder | ✅ | Create reminder |
| PATCH | /api/reminders/:id/complete | reminderController.completeReminder | ✅ | Mark complete |
| DELETE | /api/reminders/:id | reminderController.deleteReminder | ✅ | Delete reminder |
| **PROFILE** |
| GET | /api/profile | profileController.getUserProfile | ✅ | Get full profile |
| PUT | /api/profile | profileController.updateUserProfile | ✅ | Update profile |
| POST | /api/profile/emergency-contact | profileController.saveEmergencyContact | ✅ | Save contact |
| GET | /api/profile/emergency-contact | profileController.getEmergencyContact | ✅ | Get contact |
| DELETE | /api/profile/emergency-contact | profileController.deleteEmergencyContact | ✅ | Delete contact |
| POST | /api/profile/partner-invite | profileController.sendPartnerInvite | ✅ | Send invite |
| **STATIC DATA** |
| GET | /api/static/nutrition-tips | staticController.getNutritionTips | ❌ | Get tips |
| GET | /api/static/safe-foods | staticController.getSafeUnsafeFoods | ❌ | Get food lists |
| GET | /api/static/feeding-guide | staticController.getFeedingGuide | ❌ | Get guide |
| GET | /api/static/vaccine-schedule | staticController.getVaccineSchedule | ❌ | Get vaccines |
| GET | /api/static/daily-tip | staticController.getDailyTip | ❌ | Random tip |

---

## 🚀 STARTUP PROCESS

```
1. BACKEND STARTUP (npm start in /backend)
   ├─ Load environment variables (.env)
   ├─ Initialize Sequelize (db/sequelize.js)
   ├─ Connect to SQLite database
   ├─ Sync models → Create/update tables
   ├─ Register routes (server.js)
   └─ Listen on port 8000

2. FRONTEND STARTUP (npm run dev in /FrontEnd)
   ├─ Start Vite dev server
   ├─ Compile React components
   ├─ Load App.jsx with routes
   └─ Listen on port 5173

3. USER ACCESS
   └─ Browser → http://localhost:5173
      └─ React Router handles navigation
         └─ Components make API calls to localhost:8000
```

---

## 📊 KEY TECHNOLOGIES

| Layer | Technology | Purpose |
|-------|------------|---------|
| Frontend | React | UI components & state management |
| Frontend | React Router | Page navigation |
| Frontend | Fetch API | HTTP requests |
| Frontend | Vite | Build tool & dev server |
| Backend | Express.js | HTTP server & routing |
| Backend | Sequelize | ORM (Object-Relational Mapping) |
| Backend | JWT | Authentication tokens |
| Backend | bcrypt | Password hashing |
| Database | SQLite | Data persistence |
| API | REST | Communication protocol |
| Data Format | JSON | Data exchange format |

---

**Last Updated:** January 4, 2026
**Version:** 1.0
